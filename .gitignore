node_modules
.env
coverage
coverage.json
typechain
typechain-types

#Hardhat files
cache
artifacts

#yarn related
.yarn

node_modules
.env
coverage
coverage.json
typechain
typechain-types

#Hardhat files
cache
artifacts

# circom and snarkjs
circuits/build
contracts/*Verifier.sol

# ignore build files that do not need to be pushed as library
dist/scripts
dist/tasks
dist/hardhat.config*
